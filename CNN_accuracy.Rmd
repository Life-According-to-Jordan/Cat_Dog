---
title: "CNN_results"
author: "Jordan Hoehne"
date: "4/23/2018"
output: word_document
---
```{r}
#load data 
data<-read.csv("CNN_ACC.csv", header = FALSE)

#load library 
library(ggplot2)
```

```{r}
names(data)<-c("epoch", "accuracy", "model")
data
```

```{r}
ggplot(data = data, aes(epoch, accuracy)) + theme_minimal() + geom_point(aes(color = model)) + labs(title="Training and Testing Accuracy")

```
